{% extends "layout.twig" %}
{% block titleP %}
    Blog en PHP
{% endblock %}
{% block title %}
    UN CODIGO EN PHP | BLOG
{% endblock %}
{% block content %}
    {% for row in blogPosts  %}
<div class="list-group  p-2" style="align-items: center;">
        <div class="card " style="width: 40rem;">
            {% if row.img_url %}
            <img src="{{ row.img_url }}" class="card-img-top" alt="...">
           {% endif %}
            <div class="card-body">
                <h5 class="card-title">{{ row.title }}</h5>
                <p class="card-text">{{ row.content }}</p>
                <div class="position-relative">created: {{ row.created_at }} by {{ row.creador }} <a class="btn btn-info  position-absolute bottom-0 end-0"  href="#">SHOW</a></div>
            </div>
        </div>
</div>
    {% endfor %}
    <nav aria-label="Page navigation">
        <ul class="pagination justify-content-center">
            {% if page > 1 %}
                <li class="page-item enabled"><a class="page-link" href="{{ page - 1}}">Previous</a></li>
            {% else %}
                <li class="page-item disabled"><a class="page-link" href="#">Previous</a></li>
            {% endif %}


            {% for i in 1..totalPages %}
                {% if i == page %}
                    <li class="page-item active"><a class="page-link" href="{{i}}">{{i}}</a></li>
                {% else %}

                    <li class="page-item"><a class="page-link" href="{{i}}">{{i}}</a></li>
                {% endif %}
            {% endfor %}

            {% if page == totalPages %}
                <li class="page-item disabled"><a class="page-link" href="#">Next</a></li>
            {% else %}
                <li class="page-item enabled"><a class="page-link" href="{{page + 1}}">Next</a></li>
            {% endif %}

        </ul>
    </nav>
{%  endblock %}






